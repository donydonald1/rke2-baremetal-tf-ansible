apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
    name: harbor-user-secret
spec:
    refreshInterval: 1h
    secretStoreRef:
        kind: ClusterSecretStore
        name: vault
    target:
        name: harbor-user-secret
        creationPolicy: Owner
        template:
            metadata:
                labels:
                    cnpg.io/reload: ""
    data:
      - secretKey: username
        remoteRef:
            key: secret/harbor/
            property: internal_username
      - secretKey: password
        remoteRef:
            key: secret/harbor/
            property: internal_password